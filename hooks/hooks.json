{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "command -v node >/dev/null 2>&1 || exit 0; exec node \"${CLAUDE_PLUGIN_ROOT}/bin/cli.js\" hook",
            "shell": "bash",
            "timeout": 15
          }
        ]
      }
    ]
  }
}
